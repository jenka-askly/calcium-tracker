# Purpose: Document required environment variables for the Expo app (placeholders only).
# Persists: No persistence.
# Security Risks: References API endpoints; do not store secrets here.
EXPO_PUBLIC_API_BASE_URL=http://localhost:7071
